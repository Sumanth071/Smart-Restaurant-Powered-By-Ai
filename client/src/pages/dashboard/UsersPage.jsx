import EntityManagerPage from "./EntityManagerPage";
import { moduleConfigs } from "../../data/moduleConfigs";

const UsersPage = () => <EntityManagerPage config={moduleConfigs.users} />;

export default UsersPage;
